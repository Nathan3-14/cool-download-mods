name: Convert
run-name: Converting mods
on: [
    push
]
jobs:
    convert:
        runs-on: ubuntu-latest
        steps:
            - uses: actions/checkout@v4
            - run: |
                echo "Cool"
                ls
                ls src
                echo "Cool 2"
                for file in ./src
                do
                    echo "file"
                done

